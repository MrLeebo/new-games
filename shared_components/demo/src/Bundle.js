import React from 'react';

export default function Bundle() {
  return (
    <fieldset>
      This bundle was loaded using code-splitting. Look for "Bundle.demo.js" in the Network tab of your Developer Console. The code that rendered this template wasn't loaded in your browser until you clicked on that button.
    </fieldset>
  )
}
